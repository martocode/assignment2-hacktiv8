import React, { Component, useEffect } from "react";

class ScrollHighligter extends Component {
	componentDidMount() {
		const caller = () => {
			const pages = document.querySelectorAll(".pages");
			const navs = document.querySelectorAll(".nav");
			console.log(pages);
			// pages.forEach(()=>{

			// })
		};
	}
}
export default ScrollHighligter;
